name: Windows Build

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  BUILD_NUMBER: 1
  BUILD_VERSION: 1.0.0
  PYTHON_VERSION: "3.12.8"
  FLET_CLI_VERSION: "0.80.1"
  PYTHONUTF8: 1
  FLET_CLI_NO_RICH_OUTPUT: 1
  # CL: /permissive fixes the C4596 error in window_manager
  # /std:c++20 ensures modern features are available
  CL: "/permissive /std:c++20"

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      shell: powershell
      run: |
        python -m pip install --upgrade pip
        pip install flet-cli==${{ env.FLET_CLI_VERSION }}
        if (Test-Path requirements.txt) {
          pip install -r requirements.txt
        }

    - name: Flet Build Windows
      shell: powershell
      run: |
        # Clean previous failed builds to prevent cache issues
        if (Test-Path build) { Remove-Item -Recurse -Force build }
        flet build windows --verbose --build-number ${{ env.BUILD_NUMBER }} --build-version ${{ env.BUILD_VERSION }}

    - name: Upload Windows Artifact
      uses: actions/upload-artifact@v4
      with:
        name: windows-build-artifact
        path: build/windows
        if-no-files-found: error
        overwrite: true